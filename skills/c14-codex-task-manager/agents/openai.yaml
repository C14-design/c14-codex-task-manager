interface:
  display_name: "C14 Codex Task Manager"
  short_description: "Review, dispatch, and complete persistent task work"
  default_prompt: "Use $c14-codex-task-manager to coordinate this request with clear ownership and an explicit completion contract."
