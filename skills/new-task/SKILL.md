---
name: new-task
description: Compatibility entry for creating, naming, renaming, or succeeding a Codex task with a project-unique title. Use when the user invokes $new-task, asks for a fresh named Codex task, asks to rename a blank task, or asks to replace a context-heavy worker or manager.
---

# New Task Compatibility Entry

Read `../c14-codex-task-manager/SKILL.md` completely, then execute its naming, creation, retirement, and succession workflow.

Treat `$new-task` as authorization to create a fresh task. Treat `$new-task rename` as authorization to rename only the calling task. Apply project-wide collision checks and retired-task name recycling from the canonical skill.
